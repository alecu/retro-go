# fMSX BIOS files

Provide the BIOS files required by fMSX (`MSX.ROM`, `MSX2.ROM`, `MSX2EXT.ROM`,
`DISK.ROM`, and `MSXDOS2.ROM`) in this directory. They are not shipped with
VSDK. The LittleFS build copies them to `/vfs/retro-go/bios/msx/`.
