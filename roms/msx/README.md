# MSX library

Place `.rom`, `.mx1`, `.mx2`, `.dsk`, or supported `.zip` ROMs here. They are
copied to `/vfs/roms/msx/` and listed by basename in the MSX launcher submenu.

fMSX also needs its BIOS files in `../bios/msx/`; see that folder's README.
